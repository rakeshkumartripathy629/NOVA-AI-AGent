# Nova AI Backend

FastAPI backend for the Nova AI workspace.

## Run in Docker

From the repository root:

```bash
cp .env.example .env   # then fill in your keys
docker compose up --build
```

See the root `README.md` for full instructions.
